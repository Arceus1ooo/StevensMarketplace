const handleLogin = () => {
  //handle login
};

const handleLogout = () => {
  //handle logout
};

const handleSearch = () => {
  //handle search
};

const handleFilters = () => {
  //handle filters
};
